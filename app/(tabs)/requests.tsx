import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const requests = () => {
  return (
    <SafeAreaView>
      <Text>requests</Text>
    </SafeAreaView>
  );
};

export default requests;
