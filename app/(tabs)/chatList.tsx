import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const chatList = () => {
  return (
    <SafeAreaView>
      <Text>chatList</Text>
    </SafeAreaView>
  );
};

export default chatList;
